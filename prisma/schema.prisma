generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("postgresql://neondb_owner:npg_WbGB7iUz5IwO@ep-royal-hill-atk4aic3-pooler.c-9.us-east-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require")
}

model Intervention {
  id              String   @id
  date            String
  engin           String
  categorie       String
  lubrifiant      String
  compteurHoraire Int
  type            String
  quantite        Float
  responsable     String
  observation     String?
  createdAt       DateTime @default(now())
}